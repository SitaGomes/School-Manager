name: Build, Test, and Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Cache Node.js modules
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        working-directory: implementação/backend
        run: npm install

      - name: Set up environment
        run: echo "DATABASE_URL=${{ secrets.DATABASE_URL }}" > implementação/backend/.env

      - name: Run Prisma migrations
        working-directory: implementação/backend
        run: npx prisma migrate deploy

      - name: Run tests
        working-directory: implementação/backend
        run: npm test

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: success()

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to Docker
        working-directory: implementação/backend
        run: |
          docker build -t school-manager .
          docker run -d -p 3000:3000 --env-file .env school-manager

      - name: Clean up old Docker containers
        run: |
          docker container prune -f
